import SimpleLightbox from "simplelightbox";
import { getPhoto } from "./getphotos";

export function createPhoto(image){
    const li = image
    .map((photo) => `<li class="list-item">New file</li>`)
    .join("");

    
}
